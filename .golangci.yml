version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - revive
    - staticcheck
    - unused

  settings:
    revive:
      rules:
        - name: exported

formatters:
  enable:
    - gofmt
